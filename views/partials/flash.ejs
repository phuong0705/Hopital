<%
  const serverFlashNotifications = Array.isArray(flashNotifications) ? flashNotifications : [];
%>
<div
  class="flash-stack"
  id="flashStack"
  aria-live="polite"
  aria-atomic="false"
  data-flash='<%- JSON.stringify(serverFlashNotifications).replace(/</g, '\\u003c') %>'>
</div>
